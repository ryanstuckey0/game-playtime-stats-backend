spring:
  datasource: 
    url: jdbc:h2:mem:dev;INIT=CREATE SCHEMA IF NOT EXISTS statsaga
  jpa:
      properties:
          hibernate:
              default_schema: statsaga
      hibernate:
        ddl-auto: create-drop