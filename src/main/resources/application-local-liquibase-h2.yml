spring:
  datasource:
    url: jdbc:h2:tcp://localhost:9090/mem:dev;DB_CLOSE_ON_EXIT=FALSE
    username: backend_user
    password: letmein
  jpa:
    properties:
        hibernate:
            default_schema: statsaga
    hibernate:
      ddl-auto: validate