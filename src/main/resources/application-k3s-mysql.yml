spring:
  datasource:
    url: jdbc:mysql://rp5:30001/statsaga
    username: backend_user
    password: mydatabasepassword
  jpa:
    hibernate:
      ddl-auto: validate
logging:
  level:
    com.ryanstuckey0.statsaga: DEBUG